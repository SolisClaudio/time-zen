import {  Cutive_Mono } from 'next/font/google';

export const Mono = Cutive_Mono ({ 
                                    subsets: ['latin'],
                                    weight: ['400'],
                                });

// Rest of the code...